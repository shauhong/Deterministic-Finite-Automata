const Header = () => {
    return (
        <header className="bg-neutral-900">
            <nav className="w-full container mx-auto flex px-2 py-6">
                <span className="font-semibold text-xl text-white">Deterministic Finite Automata</span>
            </nav>
        </header>

    )
}

export default Header;